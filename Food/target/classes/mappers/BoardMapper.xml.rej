diff a/Food/target/classes/mappers/BoardMapper.xml b/Food/target/classes/mappers/BoardMapper.xml	(rejected hunks)
@@ -97,7 +97,7 @@
 
 <!-- 밥친구 취소하기 버튼 눌렀을때 -->
 <update id="mealcancel" parameterType="board">
-	UPDATE board SET f_cnt = f_cnt-1 ,f_userId = #{f_userId} WHERE boardType=#{boardType} AND b_no=#{b_no} AND f_cnt != 0
+	UPDATE board SET f_cnt = f_cnt-1 ,f_userId = #{f_userId} WHERE boardType=#{boardType} AND b_no=#{b_no}
 </update>
 
 <!-- qna 게시판 답글 insert -->
