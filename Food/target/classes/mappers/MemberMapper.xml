<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberDAO">




<select id="selectMember" parameterType="member" resultType="member">
		SELECT * FROM foodmember WHERE m_id = #{m_id}
		<if test="m_pass!=null">
		AND m_pass = #{m_pass}
		</if>
</select>

<select id="selectID" parameterType="member" resultType="member">
		SELECT  * FROM foodmember WHERE m_id = #{m_id}
</select>

<select id="find_id" parameterType="String" resultType="String">
		select m_id from foodmember where m_email = #{m_email}
	</select>




<insert id="insertMember" parameterType="member">
insert into foodmember(m_id,m_name,m_pass,m_tel,m_birth,m_area,m_most,m_email) 
values(#{m_id},#{m_name},#{m_pass},#{m_tel},#{m_birth},#{m_area},#{m_most},#{m_email})
</insert>
</mapper>